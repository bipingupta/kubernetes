=============================
Helpful Vagrant Commands
=============================
vagrant up                ==>> # If running first time, runs all tasks within provisioning otherwise just starts the VM and ignores all tasks
vagrant status

vagrant provision         ==>> # Runs all tasks within provisioning without restarting the VM

vagrant ssh rabbitmq    ==>> # Login to Linux box
exit                      ==>> # Exit from Linux box

vagrant suspend rabbitmq  ==>> # This suspends the guest machine Vagrant is managing, rather than fully shutting it down or destroying it.
vagrant resume rabbitmqr ==>> # resume the save machine

vagrant halt rabbitmq   ==>> # shuts down the running machine

vagrant destroy -f
vagrant box remove

vagrant plugin list
vagrant plugin install vagrant-cachier
vagrant plugin install hostmanager

vagrant reload            ==>> # Does not run provisioning but restarts the VM
vagrant reload --provision  ==>>  # Runs all tasks within provisioning with restarting the VM

=============================
Helpful My-SQL Commands
=============================
sudo service mysql restart
mysql -u root -proot

mysql -u username -proot < example.sql

SHOW DATABASES;
CREATE DATABASE testdb;
SHOW CREATE DATABASE testdb;
USE testdb;
DROP DATABASE testdb;
EXIT
GRANT ALL PRIVILEGES ON *.* TO 'username'@'localhost' IDENTIFIED BY 'password';

http://172.42.42.42:8306/phpmyadmin
